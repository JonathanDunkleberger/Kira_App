###############################################
# Root Frontend + Shared Example Environment  #
###############################################
# Copy to `.env.local` for local development. Do NOT put secrets here.
# Backend/server-only secrets have moved to: packages/socket-server/.env.example

# ---------- Public (NEXT_PUBLIC_*) ----------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_example
NEXT_PUBLIC_APP_URL=https://your-vercel-app.vercel.app
NEXT_PUBLIC_WEBSOCKET_URL=wss://your-socket-server.example.com

# ---------- Non-secret App Settings ----------
FREE_TRIAL_SECONDS=900
PRO_SESSION_SECONDS=7200

# ---------- Deployment Convenience ----------
APP_URL=https://your-vercel-app.vercel.app
ALLOWED_ORIGIN=https://your-vercel-app.vercel.app

# Server-only: DATABASE_URL, DIRECT_URL, DEEPGRAM_API_KEY, OPENAI_API_KEY, AZURE_SPEECH_KEY, AZURE_SPEECH_REGION,
# STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID, CLERK_SECRET_KEY, CLERK_WEBHOOK_SECRET
# are now defined in packages/socket-server/.env.local (see its example file).