# ---------- OpenAI (STT + LLM) ----------
OPENAI_API_KEY="sk-..."

# ---------- Gemini (LLM optional) ----------
GOOGLE_GEMINI_API_KEY="..."
LLM_PROVIDER="openai"            # 'openai' or 'gemini' (defaults to openai)
OPENAI_MODEL="gpt-4o-mini"       # optional override
GEMINI_MODEL="gemini-1.5-flash"  # optional override

# ---------- Azure TTS ----------
AZURE_SPEECH_KEY="..."
AZURE_SPEECH_REGION="eastus"
AZURE_TTS_VOICE="en-US-AshleyNeural"
AZURE_TTS_RATE="+25%"
AZURE_TTS_PITCH="+25%"

# ---------- Supabase ----------
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="public-anon-key"
SUPABASE_SERVICE_ROLE_KEY="service-role-key"

# ---------- Stripe ----------
STRIPE_SECRET_KEY="sk_live_or_test_..."
STRIPE_PRICE_ID="price_..."           # Product price for $1.99
STRIPE_WEBHOOK_SECRET="whsec_..."     # From Stripe CLI or dashboard

# ---------- App ----------
APP_URL="http://localhost:3000"       # Change to https://yourdomain.com on deploy
FREE_TRIAL_SECONDS="1200"             # 20 minutes
PRO_SESSION_SECONDS="1800"            # 30 minutes (Pro session cap per conversation)
ALLOWED_ORIGIN="http://localhost:3000"

# ---------- Dev ---------
# Set to 1 to allow local testing without Supabase auth (use with caution)
DEV_ALLOW_NOAUTH="0"

# ---------- Voice / WebSocket ----------
NEXT_PUBLIC_WEBSOCKET_URL="ws://localhost:8080"
NEXT_PUBLIC_VOICE_WS_URL=""

# ---------- Deepgram (Streaming STT) ----------
DEEPGRAM_API_KEY="dg_..."

# ---------- TTS Provider Switch ----------
# Set TTS_PROVIDER to 'azure' (default) or 'elevenlabs'
TTS_PROVIDER="azure"
ELEVENLABS_API_KEY=""
ELEVENLABS_VOICE_ID="21m00Tcm4TlvDq8ikWAM"
ELEVENLABS_MODEL="eleven_monolingual_v1"

# ---------- Clerk Auth ----------
CLERK_PUBLISHABLE_KEY="pk_..."
CLERK_SECRET_KEY="sk_..."
CLERK_WEBHOOK_SECRET="whsec_..."

# ---------- Prisma / Database ----------
DATABASE_URL="postgresql://user:pass@localhost:5432/kira"

# ---------- Usage Limits (Legacy / Migration) ----------
FREE_DAILY_LIMIT="900"             # 15 minutes total per day for free plan
PRO_CHAT_SESSION_LIMIT="1800"      # 30 minute cap per pro session
