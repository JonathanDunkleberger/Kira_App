###############################################
# Root Frontend Environment Example           #
###############################################
# Copy to `.env.local` and fill in as needed.
# Only public/non-secret vars live here. All server secrets belong in `packages/socket-server/.env.local`.

# ---------- Public Runtime Config ----------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
NEXT_PUBLIC_APP_URL=https://kira-ai-2.vercel.app
NEXT_PUBLIC_WEBSOCKET_URL=wss://kira-socket-server.onrender.com

# ---------- Non-secret App Settings ----------
FREE_TRIAL_SECONDS=900
PRO_SESSION_SECONDS=7200

# ---------- Derived / Deployment ----------
APP_URL=https://kira-ai-2.vercel.app
ALLOWED_ORIGIN=https://kira-ai-2.vercel.app

# NOTE: Server-only secrets: DATABASE_URL, DIRECT_URL, DEEPGRAM_API_KEY, OPENAI_API_KEY,
# AZURE_SPEECH_KEY, AZURE_SPEECH_REGION, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID,
# CLERK_SECRET_KEY, CLERK_WEBHOOK_SECRET go in `packages/socket-server/.env.local`.