###############################################
# Socket Server Environment Example           #
###############################################
# Copy to `.env.local` in this directory and fill real values.
# Only the keys actually used in current `.env.local` are listed.

DATABASE_URL=
DIRECT_URL=
DEEPGRAM_API_KEY=
OPENAI_API_KEY=
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# (If you need Clerk, Stripe, or other secrets later, extend both
# `.env.local` and this example in lockstep.)
